set echo on;

alter session set nls_language = english;
alter session set nls_date_format = 'DD-MON-YYYY';
alter session set nls_date_language = english;


/* SELECT COUNT(*) 3 PARTITIONS */
SELECT COUNT(*) FROM BUCHUNG;


/* SELECT GROUP BY 3 PARTITIONS */
SELECT BUCHUNG.FLC, COUNT(*) FROM BUCHUNG GROUP BY BUCHUNG.FLC;


/* SELECT GROUP BY WHERE 3 PARTITIONS */
SELECT BUCHUNG.FLC, SUM(BUCHUNG.MEILEN) FROM BUCHUNG
GROUP BY BUCHUNG.FLC;


/* SELECT GROUP BY WHERE 3 PARTITIONS */
SELECT PASSAGIER.LAND, COUNT(*) FROM PASSAGIER
GROUP BY PASSAGIER.LAND;


/* SELECT GROUP BY WHERE 3 PARTITIONS */
SELECT PASSAGIER.GEBORT, COUNT(*) FROM PASSAGIER
GROUP BY PASSAGIER.GEBORT;


rollback;