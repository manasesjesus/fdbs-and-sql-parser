set echo on;

alter session set nls_language = english;
alter session set nls_date_format = 'DD-MON-YYYY';
alter session set nls_date_language = english;


/* COUNT 3 PARTITIONS */
SELECT COUNT(*) FROM PASSAGIER;


/* COUNT 3 PARTITIONS */
SELECT COUNT(*) FROM BUCHUNG;


/* COUNT 3 PARTITIONS */
SELECT COUNT(*) FROM FLUG;


/* 2 TUPLES FROM 2 PARTITIONS DISJUNCTIVE EXPRESSION */
SELECT BUCHUNG.PNR, BUCHUNG.VON, BUCHUNG.NACH FROM BUCHUNG
WHERE (BUCHUNG.PNR = 2) OR (BUCHUNG.PNR = 36);


/* TUPLES FROM 1 PARTITION CONJUNCTIVE EXPRESSION */
SELECT BUCHUNG.PNR, BUCHUNG.VON, BUCHUNG.NACH FROM BUCHUNG
WHERE (BUCHUNG.PNR > 2) AND (BUCHUNG.PNR <= 4);


/* TUPLES FROM ALL PARTITIONS CONJUNCTIVE EXPRESSION */
SELECT BUCHUNG.BNR, BUCHUNG.PNR, BUCHUNG.VON, BUCHUNG.NACH, BUCHUNG.MEILEN, BUCHUNG.PREIS FROM BUCHUNG
WHERE (BUCHUNG.PNR > 2) AND (BUCHUNG.PNR <= 4);


/* TUPLES FROM ALL PARTITIONS DISJUNCTIVE EXPRESSION */
SELECT BUCHUNG.BNR, BUCHUNG.PNR, BUCHUNG.TAG, BUCHUNG.MEILEN, BUCHUNG.PREIS FROM BUCHUNG
WHERE (BUCHUNG.PNR = 2) OR (BUCHUNG.PNR = 79);


rollback;