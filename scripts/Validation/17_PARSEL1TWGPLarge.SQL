set echo on;

alter session set nls_language = english;
alter session set nls_date_format = 'DD-MON-YYYY';
alter session set nls_date_language = english;


/* SELECT GROUP BY */
SELECT FLUGLINIE.ALLIANZ, COUNT(*) FROM FLUGLINIE GROUP BY FLUGLINIE.ALLIANZ;


/* SELECT WHERE GROUP BY */
SELECT FLUGLINIE.LAND, COUNT(*) FROM FLUGLINIE
WHERE (ALLIANZ = 'Star') GROUP BY FLUGLINIE.LAND;


/* SELECT CONJUNCTIVE WHERE GROUP BY */
SELECT BUCHUNG.NACH, COUNT(*) FROM BUCHUNG
WHERE (BUCHUNG.MEILEN > 4100) AND (BUCHUNG.BNR > 100950)
GROUP BY BUCHUNG.NACH;


/* SELECT DISJUNCTIVE WHERE GROUP BY */
SELECT BUCHUNG.VON, COUNT(*) FROM BUCHUNG
WHERE (BUCHUNG.MEILEN > 16000) OR (BUCHUNG.BNR < 10)
GROUP BY BUCHUNG.VON;

rollback;
  